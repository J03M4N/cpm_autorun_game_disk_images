Terminal: VT52

CPMtools diskdef:

diskdef msx2
  seclen 512
  tracks 160
  sectrk 9
  secbase 65
  blocksize 4096
  maxdir 128
  skew 1
  boottrk 2
  os 3
end

HxC raw .IMG:

84        number_of_track
2         number_of_side
IBM_MFM   format
1         start_sector_id
9         sector_per_track
512       sector_size
0         formatvalue
255       gap3
250000    bitrate
0         pregap
300       rpm

Source cpm boot disk image from: http://www.z80.eu/msx2.html

Workflow: load source .IMD in HxC, export .IMG, use CPMtoolsGUI to edit .IMG, use 'load raw image' in HxC with values from above, export as .IMD.

Autostart: CPM3, so support for SUBMIT PROFILE built-in.

MAME276 command line:

mame vg8235 -flop1 "[path to cpm boot disk image]" -window -skip_gameinfo
