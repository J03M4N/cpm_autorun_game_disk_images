terminal: ADM-31...mostly, a few games just won't work right.

Cpmtools diskdef:

diskdef smc70
  seclen 256
  tracks 70
  sectrk 16
  blocksize 2048
  maxdir 64
  skew 3
  boottrk 2
  os 2.2
end

HxC raw .IMG:

70         number_of_track
1          number_of_side
IBM_MFM    format
1          start_sector_id
16         sector_per_track
256        sector_size
0          formatvalue
255        gap3
250000     bitrate 
0          pregap
300        rpm
		
Source cpm boot disk image from MAME software list.

Workflow: load source .IMD in HxC, export .IMG, use CPMtoolsGUI to edit .IMG, use 'load raw image' in HxC with values from above, export as .IMD.

Autostart: I had to remount for "read this image write to another" dialogue, rename to .MFM. ran setup to set colors, cursor and autostart, set autostart to "SUBMIT PROFILE" works perfect, just edit "PROFILE.SUB" (needs to be padded as usual in cpm2.2). then loaded the newly created .MFM with HxC and exported as .IMD Used new .IMD as source disk for standard workflow.

MAME276 command line:

mame smc777 -flop1 "[path to cpm boot disk image]" -window -skip_gameinfo	
